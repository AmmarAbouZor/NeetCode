### Kahn's DFS topological sort:
* Remember to count resolved items and compare resolved count to total count as it means that we have cycles.
* Make sure to keep the order dependency -> dependent in adj and indegree lists.
